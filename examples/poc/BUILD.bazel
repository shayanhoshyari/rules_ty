load("@pip//:requirements.bzl", "requirement")
load("@rules_python//python:py_binary.bzl", "py_binary")

py_binary(
    name = "bench_all",
    srcs = ["bench_all.py"],
    main = "bench_all.py",
    deps = [
        "//bench_deep",
        "//bench_heavy",
        "//bench_wide",
    ],
)
